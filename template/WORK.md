# {{GAME_NAME}} — work tracking

Single source of truth for all work in this game. The dev loop (headless
continuous workers or the interactive `/spraxel-develop` skill) builds from
the top of `## Up-and-coming work` and ships items into `## Finished since
last release`; you wake up to a MORNING.md digest. Deferred `[future]`
roadmap items live in `## Next work`; release cuts externalize the finished
section to `WORK_v<version>.md` archives.

Format spec: `~/SpraxelAiCompany/docs/WORK_MD_FORMAT.md`.

## Up-and-coming work

==================================================
## Finished since last release

==================================================
## Next work

==================================================
## Shipped (previous releases) — archived to WORK_v*.md (read on demand via release notes / git log)
